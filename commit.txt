Mirror the secp256k1 field-arithmetic fix from G-mOMonadOS

Same module, byte-identical: corrects the prime P low limb
(0xFFFFFFFEFFFFFC2F), the add_mod carry-doubling, the sub_mod borrow fold, and
the mul_mod dropped fold carry. recover and verify now return correct keys for
all tested k, even and odd, verified against a python oracle. Builds bare-metal
and hosted; identical to G-mOMonadOS.
