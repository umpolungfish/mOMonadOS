Rebuild: nine QC modules were in source and unreachable

braid_grammar, afdmc, hop, troq, manifold, dyson, hqe, repl and main were all
newer than the built ELF, repl.rs by several hours. Their REPL arms existed in
source and answered "Unknown: bg" in the running kernel, so the whole
m3iosis-mirror surface was present and not callable.

Rebuilt. All ten QC commands now respond: fibqc, bg, shor, iuft, hqe, dyson,
troq, afdmc, hop, manifold, triple.

Worth recording from the retest: `bg tuple` gives the same tuple with Omega = awe
for all three spellings of the identity braid on seven strands. The Python bridge
got that wrong until today, because it summed eigenvalue phases wrapped into
[0,360) and an eigenvalue at zero-from-below contributed a spurious full turn.
The kernel computes topological_spin as writhe*2/5 mod 1 — a closed form in the
writhe that never diagonalises anything — so it cannot acquire that error.

The help block does not list bg, hqe, dyson, troq, hop or afdmc yet.
