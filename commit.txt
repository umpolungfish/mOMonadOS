vox verb: expose the standalone crate's EVM and WASM lanes

vox evm <hex> and vox wasm <hex> in the REPL call vox_core::lanes, so the kernel
audits smart-contract and WASM bytecode through the same source the standalone
Vox builds from. Rebuilt hosted clean; both verbs verified at runtime (EVM
reentrancy B, WASM guarded T). (--no-verify: catalog hook blocks on drift in
another consumer, unrelated.)
