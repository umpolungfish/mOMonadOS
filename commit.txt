The closure witnesses vote: the Grammar grounded the affinity reading and refused the gate.

Put as two statements, the pair discriminated. The reading that makes R4 through R7 four further affinity columns grounds full, with Sigma at the symmetric value and the witnesses typed as catalytic stabilizers that modulate the vote without being consumed. The reading that makes them a gate fails to ground, and types Sigma asymmetric and the witnesses as a control signal. Both tuples agree on Omega; they part at Sigma, at T and at chirality.

So register_scores now reads all eight registers. The witness block mirrors the tuple block rather than carrying weights of its own, which is what voting in the same manner means and which keeps the extension derived rather than invented: each token's affinity for a witness register is its affinity for the tuple register in the same position, and the two contributions add.

What this makes true of the substrate: a program's diversity, self-reference, Frobenius order and dialetheia completeness now pull the next token toward the primitives those registers favour, so closure evidence feeds back as preference rather than as a precondition.
