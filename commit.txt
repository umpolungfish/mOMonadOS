Adopted the corrected repair as PROTOCOL_WORD: swap TANCH and FFUSE
(positions 1 and 8) instead of inserting an extra VINIT. Same multiset of
tokens as the ob3ect's original word, no insertion, verified against all
three instruments now that candidate_holds checks all three: banked_walk
holds, tri_ancestral_word_verdict T, check::word_verdict grammar-legal.
check's own closure reading is B/Open, not T, and that's real, not a
downgrade to paper over -- its pairing is a linear stack with no
wraparound, tri_ancestral's is cyclic, and this word puts a FFUSE at tick
2, before any FSPLIT has fired. Ran it live: tick 2 of gpu_native_cycle
shows exactly that, OBSERVE closing None, a fuse with nothing pending to
close. Reran run/run_real/run_chained/run_cycle against the new word on
the RTX 4070, all four clean, zero mismatches against the CPU where they
check one.
