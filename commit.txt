Regenerate against the stripped catalog

One entry moved into the crystal when the compatibility layer came out:
axiom_of_maria's topology slot held a retired value spelling, so it had no legal
address and the generator skipped it. It emits now — 8,017 entries, up from
8,016, and none fewer.

Boot still reports ordinal faithfulness against the Lean canonical.
