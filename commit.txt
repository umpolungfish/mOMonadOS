straus: the instrument reads the whole price-zero layer

classify was calling everything but n = 5 (mod 8) an iterated walk, when three
rungs are read straight off n: r = 3 (mod 4) dividing n, n+1 or n+4. Those are
one-shots and now report as one, each naming which family it is and the divisor
it commits to.

New verb, straus frontier <lo> <hi>: the values both zero-price layers miss, the
ones whose divisor has to come from a prime of a. Over 5 <= n <= 200000 that is
1271 of 33333, all of them n = 1 (mod 24). The census over 5..4000 moves from
50.2% one-shot to 93.4%.
