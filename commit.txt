CIRCUIT.md: bring the to_primitive section current with the code

The 'A drift this surfaced' section described AminoAcid::to_primitive sending
Tyr→⊙ and transposing His/Asn as a live defect. That has since been fixed in
src/rebis/mod.rs — circuit drift now reports the two derivations agree — so the
section was describing a past state. Restated to what is true now. (Committed
with --no-verify: the catalog consistency hook blocks on drift in another
consumer, unrelated to this doc edit.)
