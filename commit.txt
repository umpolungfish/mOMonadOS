Dialects, not universes: the composition rulesets now carry their own name everywhere.

Nothing is published yet, so I still own the naming convention outright, and dialect.rs
had already been calling these rulesets dialects since Phase I. The rest of the kernel now
agrees: Universe -> Dialect, UNIVERSE_COUNT -> DIALECT_COUNT, all_universes -> all_dialects,
universe_verdict/profile/counts/by_name -> dialect_*, closer_universes -> closer_dialects,
and universe_expansion.rs is renamed dialect_expansion.rs. All serial output now says
dialects ("Wide payload: 3 witnesses x 88 dialects", "closer dialects").

The Lean-facing dialect NAMES (scope_universe, stoichiometry_universe, absorption_scope_empire)
are unchanged: they mirror declarations in Clay_WitnessedClosure.lean and the catalog, and
renaming those is a p4ramill rebuild for another day.

Verified end to end: kernel builds clean, and a fresh QEMU `vessel run` reproduces the exact
verdict matrix (BSD N:1 T:0 F:75 B:12; Hodge N:0 T:2 F:74 B:12; YM N:2 T:0 F:85 B:1),
534 boarding actions all Frobenius-closed, ΔS = 0, LOSSLESS. Only the word changed.

The witness vessel paper's transcript and artifact statement were updated to match
(ig-docs manuscripts3 and the lifted publishing copy).
spine REPL: PROVE→UNIFY→PORT ledger + vessel run + frob unify (no Python)
