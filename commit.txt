witness-vessel runtime half: the OS boards its own Witnesses and agrees with its own theorem.

My witness-vessel protocol now has both halves. The Lean half (p4rakernel SIC_D12_WitnessVessel.lean) proved the Dual-Link SIC-POVM transports MPP Witness payloads losslessly; this commit is the runtime half, verified in QEMU: VERDICT LOSSLESS, every Frobenius check closed, delta-S zero, gate verdicts identical before and after the round trip.

New src/witness_vessel.rs. The payloads are computed, never hand-entered: each Clay Witness verdict comes from its canonical catalog tuple through the closer-universe gates (chirality_first, scope_universe, kinetics_trap, the absorption pair for BSD; scope/kinetics/stoichiometry/absorption for Hodge; triple_criticality for YM) with the Clay T_CEILING as the T side - BSD -> T, Hodge -> T, and Yang-Mills -> B, the dialetheia derived on bare metal exactly as ymVerdict_B derives it by native_decide. Boarding is FSPLIT, read-back is FFUSE, and both substrates carry every value: the ParaVM belief instructions (B bifurcates to (T,F) - the Witness rides both arms) and the graph kernel's ForkFrames (the un-gated right arm carries the cargo, EVALT gates the left arm, FFUSE joins). frob_verify's mu-after-delta law gates every single boarding action before the loop advances.

The vessel is also the first consumer of the full 88-universe expansion: the wide payload is 3 Witnesses x 88 universes, each universe judging by its OWN gates and T-constitution. The matrix makes the structure visible - YM's row holds exactly one B, at triple_criticality; BSD goes B at precisely its five closer universes (gate closed, exact-match T_CANONICAL blocked, T_CEILING clear). New REPL command: vessel [run].

Also brought src/d12_sic.rs up from cont.19 to the capstone state: the embedding is COMPLETE (zero sorries, wrong-root bug fixed - g0 is the negative branch, IVT-bracketed), crystal_forces_d12_sic is a THEOREM with the axiom retired from SIC_POVM_Functor, audit clean; the stale in-progress/8-sorries/deg-288 claims are corrected (the fiducial's true home is the existence ring, dim 2048/Q; the ray tower stays as the discovery trail). d12 gains embedding/symmetric/lean-status subcommands that existed but were never dispatched.
