erdos straus and oddcycle

straus reads the price-zero layer through the instrument that carries it: 3248 of
the 3333 values of the surviving class below 20000 close with the rung read off n
rather than searched, and 2521 comes back at rung 23 with no cofactor of M there,
which is what puts its divisor in M^2.

oddcycle searches all 1024 two-colourings of K5 and finds none leaving both
classes bipartite, so the bound of four vertices at two colours is tight, then
searches all 32 side assignments of C5 and finds no proper split. The step that
stood first counted to 2^n, which is arithmetic rather than the theorem.

Fourteen walks. Every manuscript but four now boots.
