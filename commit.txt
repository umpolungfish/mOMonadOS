Renamed shor_sim.rs to shor_qft.rs, and the REPL command from shor-sim
to shor-qft. Lando's point stands: it isn't a simulation of Shor's
algorithm, it is Shor's algorithm, run on a real complex-amplitude
statevector because that's how you check it without quantum hardware --
"sim" was hedging a result that stands on its own. Wanted bare "shor"
for the command but that name is already the existing Belnap-Shor
pipeline's namespace (factors/gap/phase/ring/fib/dialetheic), so
shor-qft names the actual mechanism instead of a qualifier.

Re-ran both textbook cases under the new name to confirm nothing broke
in the rename: a=7,N=15 and a=3,N=91 both still extract the correct
period and recover the correct factors, verified.
