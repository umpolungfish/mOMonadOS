Clear two secp256k1_unwinder warnings

Drop the redundant mut on base in powmod (it is never reassigned), and gate the
arithmetic mod tests with #[cfg(test)] so it no longer compiles into the release
build, which is what flagged its use super::* as unused. Both repos identical.
