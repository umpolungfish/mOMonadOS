Verify Welch where a fiducial exists

|<psi|D psi>|^2 = 1/(d+1) is a property of a VECTOR, and the report was
computing 1/2049 from 2048, which asserts nothing about whether any vector
attains it. The d=12 fiducial recovered in d12_sic_build attains it, so the
kernel now displaces that vector through the whole Weyl-Heisenberg group and
measures all 143 overlaps: worst deviation 6.4e-16 in f64, and the same
coordinates give 4.5e-201 at 200 digits.

At 2048 the check has nothing to run on and the report says so. That campaign's
numerical route is closed at f ~ 0.7817 against 0.9967 for random, and the
arithmetic route is blocked on L-values at conductor p^12*inf_1 -- bnrL1
OOM-killed at 38.6 GB, lfun overflowing 28 GB for one character, and the ascent
walk superpolynomial with a climbing exponent. Welch is verified at 12 and open
at 2048.

The fiducial is carried in, not reconstructed. It is that campaign's result and
this is the kernel checking the property it is supposed to have.

fibqc verify all pass. Both trees carry the same file, help string updated.
