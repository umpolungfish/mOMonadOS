Synced the compiled catalog to canon. The 12 named entries where the
compiled table disagreed with the live IG_catalog.json split into two
groups: 9 live in catalog.rs's hand-written STATIC_CATALOG (clink_l8, the
four clink_layer0-3 entries, temporal_mathematics,
universal_imscriptive_grammar, electron, proton), hand-fixed each entry()
call's IgPrim arguments to the tuple IG_catalog.json's own Shavian fields
decode to, checked against the same primitive_from_glyph the compiled
table's own codegen uses, not a second decoder that could drift from it.
The other 3 (zfc_set_theory, e8_aether, g2_vessel) live in the generated
catalog_ask_subset.rs; reran tools/gen_catalog_subset.py, the same fix
already used once before on this file.

Reran gpu_ipc_no_serialization's cross-check after: the 9 hand-fixed
entries now agree exactly. proton's canon JSON description is the bare
word "proton", where every other entry here (and the compiled one it
replaced) carries real prose -- flagged, not treated as equivalent to the
other 8, but still applied since the tuple itself decodes cleanly and the
instruction was that the JSON is canon.

The 3 subset entries are still real mismatches, a different problem: the
live JSON has a second, differently-cased copy of each name
(G2_vessel/E8_aether/ZFC_set_theory alongside g2_vessel/e8_aether/
zfc_set_theory), genuinely different content under each pair, not
duplicates. gen_catalog_subset.py's collision handling lowercases every
name before comparing, so it treats the pair as one entry and keeps
whichever came first in the JSON array -- which happened to be the
capitalized one both times, so the lowercase name in the compiled table
now holds the capitalized entry's content, not its own. Left alone,
not a call to make silently.
