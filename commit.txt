Built the GPU-native-build-of-mOMonadOS ob3ect for real: new module
gpu_native_protocol.rs runs its repaired protocol word as actual GPU
hardware actions, not a description of what one would do.

That ob3ect's own word closes (tri-ancestral T) but its own banked_count_check
failed at step 11, the ≺ step it had already, independently, named
"hardware-locked reverse path... blocked by topological chirality." Its
repair search found 8 candidates and marked all 8 as holding. Checked the
first one live against tri_ancestral_word_verdict before writing a line of
GPU code and it does not actually hold: inserting a second ∈ with nothing
to pair it fixes the banked count and breaks the fork/fuse balance, flipping
the verdict from T to B. True of all 7 ∈-insertion candidates on the list by
simple count, 2 opens against 1 close balances under no cyclic rotation, and
the repair search never checked for it because it only ever asked
banked_walk. The one candidate that isn't an ∈ insertion -- ⊢ inserted
between ⊞ and ∋ -- doesn't touch the fork/fuse count. Checked that one
too, live: T and banked_ok both hold.

That word is what actually runs now. Two passes, each a real CUDA kernel
launch where every thread branches on its own lane parity (∈, real
divergence, not simulated), both arms' counts read back un-reduced (⊞,
literally holding both rather than collapsing to one bit), then a real
attempt to invert the aggregate back to the per-lane assignment (≺) that
is provably blocked whenever thread count exceeds 2 -- the aggregate has
strictly less information than what produced it, demonstrated rather than
asserted -- and a monotonic, never-decremented counter (⊡) closing each
pass. Ran it at a million threads per pass on the RTX 4070: exact 500000/500000
split both passes, both instrument checks held before anything launched.

Full test suite: 259 passed, 0 failed. Bare-metal build still compiles clean.
