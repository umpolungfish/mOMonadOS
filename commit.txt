d2048: the fiducial is not pending, and the report said it was

The d2048 report printed the class-field ascent, ended on "L7+: PENDING —
ramified (2048)*oo, 2^21 steps to moduli field", and stopped. Read plainly that
says the d=2048 fiducial is still out of reach. It is not, and has not been
since 2026-07-30, when the 2-part structural S-unit bypass extracted it exactly:
Stark unit eps = (2047 + sqrt 4190205)/2 satisfying x^2 - 2047x - 1 = 0,
exponents [-1,3,2], verified to 1000 digits. The bypass goes AROUND the ramified
layers, which is the whole point of it — numerical optimisation caps near 22%
frame potential on Omega saturation and exact algebraic extraction does not.

I found this because the closure specialist read the report and concluded the
fiducial was pending. It quoted the kernel correctly. The kernel was describing
a route that had been superseded four months earlier, which is the third time
today the same shape of thing has surfaced: a snapshot read as canonical, a
review written before a migration, and now a status line written before a
bypass. Each was true when it was written.

The report now says the fiducial does not depend on L7+, names the bypass and
its Stark unit, and points at oneshots #11 and
ig-docs/sic_fiducial_extraction_2part_bypass.md. L7+ stays where it was, open as
the moduli-field ascent in its own right rather than as a blocker on anything.

The distinction that was missing, and is now stated wherever this is described:
pending is not failed, proved is not conjectured, and bypassed is neither. Two
states could not describe this, which is exactly why it read wrong.
