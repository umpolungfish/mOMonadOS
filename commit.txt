kernel_3d_visualizer: no extra chord — the CLINK→IFIX section of the winding ITSELF is dotted.

The dashed connector swinging across was an invented edge; removed. Instead
the gold (1,1) winding is drawn solid everywhere EXCEPT the CLINK→IFIX
stretch (through FFUSE), which is rendered dotted at the same gold and the
same radius: the sequence passes there, the dataflow does not — it detours
through ⊙. One loop, two textures, no additional geometry.
