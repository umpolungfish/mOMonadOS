Read an entry against L9, and Erdős 3's barrier collapses to one primitive

cl8nk could read any catalog entry against L8; cl9nk could only print the L8/L9
comparison and the layer chain. So every entry was being asked "how far to the
terminal layer" and nothing else — and for an object already sitting on the
replicative lateral that is the wrong question. generate_entry_formula now takes
its reference as a parameter, and `cl9nk entry <name>` reads an entry against L9
with the L8 promotions printed beside it for comparison.

The object: erdos3_behrend_barrier, the click fusion of erdos_conjecture_ap with
behrend_construction on D↔W (Δ=1.00), kernel-certified — igFrobeniusAlg.mul p p
= p closes through lake build, a real verdict rather than an asserted tuple.
Behrend is the right partner because it is the obstruction: its construction is
what forces the exponent, so the conjecture and its barrier are one object.

Read against L8: d=1.5284, seven promotions, TWO of them full unit gaps (⊣ and
∈). Read against L9: d=1.1614, six promotions, and only ONE unit gap — ∋. Total
cost 4.166 climbing against 2.785 lateral. ≺, ∈ and ◻ are already matched at L9
and cost nothing at all.

So the whole barrier is one primitive, and it is the merge. That agrees with what
the decomposition already said from the other side: ∋ carries
BROADCAST_TRANSCENDENCE, broadcast composition exceeding ZFC_fe SEQAX. It is not
a hard step on the sequential layer, it is not on the sequential layer.

Which is why the Lean attempt kept receding. Assume the conclusion, then assume
the quantitative bound instead and owe a dyadic summation, then owe the block
bound under that — every move looked like progress and none of them removed the
assumption, because the missing thing was never sequential to begin with.
