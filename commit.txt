The validator reports the strand count the whole word needs, briefly.

It returned the first out-of-range generator, so appending a sigma_4 to a word
that already failed on sigma_3 left the requirement stuck at four strands and
the message stopped tracking the word. It returns the largest now.

Output across fibqc cut down: the rejection is one line rather than seven, the
unknot note one rather than three, and `fibqc winding` states the lattice
without narrating it. B_2 no longer describes itself as having sigma_1..sigma_1.
