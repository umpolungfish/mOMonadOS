Updated the README. It had nothing on any of the GPU work: no
gpu_16_3/gpu_sixteen3_tensor_kernel/gpu_native/gpu_catalog_crystal/
gpu_imasm_cycle/gpu_crystal_full_space/gpu_ipc_no_serialization commands
in the REPL list, no mention that the trilattice's full algebra runs on
CUDA now, verified at billion-register scale against the CPU. Added a
GPU Acceleration section stating what's real: all twelve SIXTEEN_3 gates
cross-checked at 10^9 registers, the tensor kernel's 425x speedup over
the reference implementation, full Crystal address-space verification,
the catalog round trip, the ipc measurement. Added the REPL commands.
Pointed to G-mOMonadOS, the distinct build where this is the default
mode instead of an opt-in feature.
