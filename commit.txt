gen_catalog_subset.py force-lowercased every name before deduping, so any
two entries whose names differed only by case collided and only the first
in JSON array order survived, under the lowercase spelling regardless of
which one it actually was. Checked all 13 reported collisions: every one
is a genuine pair, two different descriptions and two different tuples
under names that differ only in case, not a real duplicate. Fixed the
generator to dedupe on the exact original name -- a true exact-string
duplicate still collapses to one entry as before, a case-only pair is no
longer treated as the same entry at all, and each keeps its own original
casing in the emitted Rust. Reran it: 8671 entries, all 13 pairs (26
entries) present under their own names, zero skipped for the reason that
used to drop them.

Reran gpu_ipc_no_serialization's cross-check after: 8671 entries matched
by name between the compiled table and the live JSON, 0 address
mismatches. Every one of the 12 originally-found divergences between the
compiled catalog and its own JSON source is closed now -- 9 by hand-fixing
catalog.rs's STATIC_CATALOG entries, 3 by this generator fix plus a
regen, all of them verified against the same live JSON, not assumed
fixed because the diff looked right.
