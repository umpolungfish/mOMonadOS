Port the secp256k1_unwinder descriptive module to mOMonadOS

The 19-glyph morphism module: GLYPH_WORD, the twelve phase-1 mappings, the
UnwindStep enum in phase-4 order, the WindingState/WindingRecord per-step
landings, and the finalize verdict. Registered with a mod declaration and a
Quantum menu entry, matching the G-mOMonadOS copy.
