Docs: cut the phase narrative from the README, add the EVM lane to the compute note

README loses the Phase 1 through Phase 5 running commentary. A README says what
the thing is now, not the order in which it was built, and the completed-phase
list was the latter. What it is now survives intact: the kernel's thesis, the
REPL-verb-and-IMASM-program rule, the Crystal of Types addressing, the m3iosis
mirror named as history rather than as a pattern to extend.

IMASM_NATIVE_COMPUTE gains the EVM lane: the lifter written as a parasm word and
run inside parasm, inverting plank 3, which built the IMASM word in Rust and only
verdicted it in parasm. The dispatch trie is the lifter now rather than a test
scaffold, plus the exotic one-shots.

The EVM section arrived as a replacement for the whole file rather than an
addition to it, which dropped the title and every section above it: the machine
argument for parasm realizing the twelve, the B4 alphabet, the decode table as
word rather than data, both proven planks, and the ramp to the Replicating Code.
Those are the file's thesis and they are restored, with the EVM lane appended
where it belongs.
