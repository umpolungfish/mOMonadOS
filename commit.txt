The nest reproduces on the metal

With adjacency built once in imasm_core, `prooflift nest` returns: the closure
law's own proof, tens of thousands of tokens, verdict T, in about eleven seconds
wall-clock including the boot. It previously did not return inside ten minutes,
and the substrate was the wrong suspect — the lookup was.

So the manuscript's headline verdict is now bootable rather than only trusted.
